import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// vite.config.ts
export default defineConfig({
  base: '/Yucom-Audit-System/', // 必須與您的 GitHub 倉庫名稱一致
  // ... 其他既有的設定
})
